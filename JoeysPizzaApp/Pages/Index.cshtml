@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

@foreach(var item in Model.Pizzas)
{
<div class="card mb-3" style="max-width: 100%;">
    <div class="row g-0">
        <div class="col-md-4">
            <img src="~/Images/@item.Photo" class="img-fluid rounded-start" alt="...">
        </div>
        <div class="col-md-8">
            <div class="card-body">
                <label hidden>@item.Id</label>
                <h5 class="card-title">@item.Name</h5>
@*                <p class="card-text">@item.pizzaDesc</p>
*@                <label>Price : @item.Price </label><br />
                    <a asp-page="cart" class="btn btn-primary" asp-page-handler="buynow" asp-route-id="@item.Id">Buy Now</a>
                @*<a href="#" class="btn btn-primary">About</a>&nbsp;&nbsp;<a asp-page="" href="#" class="btn btn-primary">Add to Cart</a>
                &nbsp;<label>Quantity : </label>&nbsp;&nbsp;<input type="number" name="qty" id="qty" min="1" max="10" step="1" value="1">*@
            </div>
        </div>
    </div>
</div>
}
